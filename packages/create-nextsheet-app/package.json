{
  "name": "create-nextsheet-app",
  "version": "0.2.0",
  "description": "Create a NextSheet workbook app in seconds.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "create-nextsheet-app": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "picocolors": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  }
}
